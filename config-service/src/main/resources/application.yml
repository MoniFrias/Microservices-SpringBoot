server:
  port: 8083 
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: part8-CircuitBreaker
          uri: https://github.com/MoniFrias/Microservices-SpringBoot.git
          search-paths: config-data
