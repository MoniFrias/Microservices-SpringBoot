server:
  port: 8083 
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: part7-Gateway
          uri: https://github.com/MoniFrias/Microservices-SpringBoot.git
          search-paths: config-data
