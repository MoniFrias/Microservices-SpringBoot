server:
  port: 8080
#URL Rating-service
ratingServiceFindByIdUser: http://localhost:8082/rating/getRatingByUserId/
# Eureka - True because it will be registered in eureka
eureka:
  client:
    fecth-registry: true
    registry-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
    instance:
      hostname: localhost