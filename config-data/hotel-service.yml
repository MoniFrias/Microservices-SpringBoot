server:
  port: 8081
# URL Rating-service
ratingServiceFindByIdHotel: http://localhost:8082/rating/getRatingByIdHotel/
# Eureka - True because it will be registered in eureka
eureka:
  client:
    fecth-registry: true
    registry-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
    instance:
      hostname: localhost